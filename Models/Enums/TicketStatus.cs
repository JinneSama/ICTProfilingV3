namespace Models.Enums
{
    public enum TicketStatus
    {
        Accepted = 0,
        Assigned = 1,
        OnProcess = 2,
        ForRelease = 3,
        Released = 4,
        Completed = 5
    }
}
